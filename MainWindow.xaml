<Window x:Class="ModernInjector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Advanced Process Injector Pro" Height="800" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBgBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}" 
        ResizeMode="CanResize"
        AllowDrop="True" Drop="Window_Drop">

    <Window.Resources>
        <!-- Domyślne kolory (Tryb Ciemny) -->
        <SolidColorBrush x:Key="WindowBgBrush" Color="#121214"/>
        <SolidColorBrush x:Key="CardBgBrush" Color="#1A1A1E"/>
        <SolidColorBrush x:Key="ItemHoverBrush" Color="#25252B"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#9E9EA8"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#2E2E38"/>
        <SolidColorBrush x:Key="InputBgBrush" Color="#18181C"/>
        <SolidColorBrush x:Key="ButtonBgBrush" Color="#25252B"/>
        <SolidColorBrush x:Key="ButtonHoverBrush" Color="#32323B"/>
        <SolidColorBrush x:Key="ConsoleFgBrush" Color="#4EC9B0"/>
        <SolidColorBrush x:Key="PidColorBrush" Color="#4EC9B0"/>
        <SolidColorBrush x:Key="RamColorBrush" Color="#E5C07B"/>

        <!-- Styl przycisków -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBgBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource ButtonHoverBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Styl Danger Button -->
        <Style x:Key="DangerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#5A1D1D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#8C2E2E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#6E2424"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- PEŁNY STYL COMBOBOX -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource InputBgBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                          Background="{TemplateBinding Background}" 
                                          Foreground="{TemplateBinding Foreground}" 
                                          BorderBrush="{TemplateBinding BorderBrush}" 
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          Focusable="false" 
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="templateRoot" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <Path x:Name="arrow" Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z" Fill="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="8,0,20,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>

                            <!-- Popup z listą rozwijaną -->
                            <Popup x:Name="PART_Popup" AllowsTransparency="True" Focusable="False" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" PopupAnimation="Slide">
                                <Border x:Name="dropDownBorder" Background="{DynamicResource InputBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer x:Name="DropDownScrollViewer" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Styl elementów ComboBoxItem -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource ItemHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource ItemHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="ADVANCED PROCESS INJECTOR PRO" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                <TextBlock Text="Wersja z obsługą wielu DLL, Auto-Inject, architektur i zarządzania wątkami" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
            </StackPanel>
            <Button x:Name="BtnThemeToggle" Content="☀️ Jasny Motyw" Click="BtnThemeToggle_Click" HorizontalAlignment="Right" Width="120" Height="30"/>
        </Grid>

        <!-- Pasek wyszukiwania i metody -->
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="90"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtSearch" Grid.Column="0" Height="32" Background="{DynamicResource InputBgBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" VerticalContentAlignment="Center" Padding="8,0" TextChanged="TxtSearch_TextChanged"/>
            <ComboBox x:Name="CbMethod" Grid.Column="1" Height="32" Margin="8,0,8,0">
                <ComboBoxItem Content="Standard Injection"/>
                <ComboBoxItem Content="Thread Hijacking"/>
                <ComboBoxItem Content="LdrLoadDll Stub"/>
                <ComboBoxItem Content="Manual Map"/>
            </ComboBox>
            <Button x:Name="BtnRefresh" Grid.Column="2" Content="Odśwież" Click="BtnRefresh_Click" Height="32"/>
        </Grid>

        <!-- Panel Auto-Inject oraz Opóźnienia -->
        <Border Grid.Row="2" Background="{DynamicResource CardBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>
                <CheckBox x:Name="ChkAutoInject" Grid.Column="0" Content="Auto-Inject (proces):" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox x:Name="TxtAutoInjectTarget" Grid.Column="1" Height="26" Background="{DynamicResource InputBgBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource BorderBrush}" VerticalContentAlignment="Center" Margin="0,0,15,0" ToolTip="Np. gra.exe"/>

                <TextBlock Grid.Column="2" Text="Opóźnienie:" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,6,0" FontSize="11"/>
                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider x:Name="SliderDelay" Width="100" Minimum="0" Maximum="10" Value="0" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="SliderDelay_ValueChanged"/>
                    <TextBlock x:Name="TxtDelayVal" Text="0s" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center" Margin="6,0,0,0" FontSize="11" Width="25"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ListView Procesów z kolumnami, ikonami i sortowaniem -->
        <Border Grid.Row="3" Background="{DynamicResource CardBgBrush}" CornerRadius="6" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <ListView x:Name="LvProcesses" Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}" BorderThickness="0" SelectionChanged="LvProcesses_SelectionChanged" MouseRightButtonUp="LvProcesses_MouseRightButtonUp">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="" Width="30">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Image Source="{Binding Icon}" Width="16" Height="16" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Nazwa procesu" Width="170" DisplayMemberBinding="{Binding Name}"/>
                        <GridViewColumn Header="PID" Width="70" DisplayMemberBinding="{Binding Id}"/>
                        <GridViewColumn Header="RAM" Width="90">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding MemorySize}" Foreground="{DynamicResource RamColorBrush}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Architektura" Width="85" DisplayMemberBinding="{Binding Architecture}"/>
                        <GridViewColumn Header="Główne Okno / Ścieżka" Width="310" DisplayMemberBinding="{Binding MainWindowTitle}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <!-- Dolny panel sterowania procesem -->
        <Grid Grid.Row="4" Margin="0,6,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="TxtProcessCount" Grid.Column="0" Text="Znaleziono procesów: 0" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
            <CheckBox x:Name="ChkAutoRefresh" Grid.Column="1" Content="Auto-odświeżanie (3s)" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" VerticalAlignment="Center" Margin="0,0,15,0" Checked="ChkAutoRefresh_Checked" Unchecked="ChkAutoRefresh_Unchecked"/>
            <Button x:Name="BtnKillProcess" Grid.Column="2" Content="Zabij zaznaczony proces" Click="BtnKillProcess_Click" Width="155" Height="26" FontSize="11" Style="{StaticResource DangerButtonStyle}"/>
        </Grid>

        <!-- Konsola logów + Przycisk eksportu -->
        <Border Grid.Row="5" Background="{DynamicResource InputBgBrush}" CornerRadius="6" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="6">
            <DockPanel>
                <Grid DockPanel.Dock="Top" Margin="2,0,0,4">
                    <TextBlock Text="Konsola diagnostyczna:" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                    <Button Content="Eksportuj logi" Click="BtnExportLogs_Click" HorizontalAlignment="Right" Width="85" Height="20" FontSize="10"/>
                </Grid>
                <TextBox x:Name="TxtConsoleLogs" IsReadOnly="True" AcceptsReturn="True" Background="Transparent" Foreground="{DynamicResource ConsoleFgBrush}" BorderThickness="0" FontFamily="Consolas" FontSize="11" VerticalScrollBarVisibility="Auto"/>
            </DockPanel>
        </Border>

        <!-- Zarządzanie wieloma plikami DLL (Drag & Drop) -->
        <Grid Grid.Row="6" Margin="0,8,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ListBox x:Name="LbDlls" Grid.Column="0" Height="45" Background="{DynamicResource InputBgBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource BorderBrush}" ToolTip="Przeciągnij pliki DLL tutaj lub użyj przycisku"/>
            <Button x:Name="BtnAddDll" Grid.Column="1" Content="Dodaj DLL" Click="BtnAddDll_Click" Width="95" Height="45" Margin="8,0,0,0"/>
            <Button x:Name="BtnRemoveDll" Grid.Column="2" Content="Usuń zaznaczoną" Click="BtnRemoveDll_Click" Width="110" Height="45" Margin="8,0,0,0" Style="{StaticResource DangerButtonStyle}"/>
        </Grid>

        <!-- Przycisk głównego wstrzykiwania -->
        <Button x:Name="BtnInject" Grid.Row="7" Content="WSTRZYKNIJ WSZYSTKIE DLL DO PROCESU" Click="BtnInject_Click" Height="40" Background="#0E7052" BorderBrush="#12966C" Foreground="White" FontWeight="Bold" FontSize="12"/>
    </Grid>
</Window>